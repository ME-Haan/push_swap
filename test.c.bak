#include	<stdio.h>
#include	"./libft/libft.h"
#include	"push_swap.h"


int	main (int argc, char *argv[])
{
	check_error(argc, argv);

	// int i = 1;
	// (void)argc;

	// while (argv[i])
	// {
	// 	printf("FT: %i\n", ft_atoi(argv[i]));
	// 	printf("OG: %i\n", atoi(argv[i]));
	// 	// printf("Me: %i\n", ps_atoi(argv[i]));
	// 	i++;
	// }
}
